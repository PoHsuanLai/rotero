[application]
name = "Rotero"

[bundle]
identifier = "com.rotero.Rotero"
publisher = "Rotero"
# macOS uses the .icns, Windows the .ico; the bundler picks per platform.
icon = ["assets/icon.icns", "assets/icon.ico", "assets/icon.png"]
